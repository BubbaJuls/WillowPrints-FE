# Copy to .env.local and fill in. Used by Supabase client (server + client components).

# Supabase project URL (Dashboard → Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://kpgnfrrrgkowfczspfcc.supabase.co

# Supabase anon/publishable key (Dashboard → Project Settings → API)
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-publishable-key-here

# Optional: WP-BE API base URL for REST API calls
# NEXT_PUBLIC_API_URL=http://localhost:4000
